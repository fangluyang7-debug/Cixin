{
  "name": "cixin-agent",
  "version": "0.1.0",
  "private": true,
  "description": "CIX board fleet runtime with portable Harmony scheduling policies",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "test": "npm run build && node --test test/*.test.cjs",
    "demo": "npm run build && node scripts/demo.cjs",
    "migration:verify": "node scripts/verify-migration.cjs"
  },
  "devDependencies": { "@types/node": "^22.10.2", "typescript": "^5.7.2" }
}
