# 分阶段开发与验证记录

开发分支：`harmony/mobile-scheduler`。只修改 `harmony-agent`，不合并或覆盖此芯主线以及旧初赛工作区。

## 阶段一：原生工程迁移（2026-09-20）

- 来源：旧初赛工作区 `HarmonyOS--/shopping-assistant/apps`，HEAD `37418e7f2552f6ce9df249569f4784cd3b62ca7c`，包括已有未提交修改。
- 目标：`apps/harmony`。96 个文件复制后逐一 SHA-256 校验一致。
- 包含原生应用、HAR、模型和一致的商品/向量数据。排除 SDK、依赖、缓存、构建产物、签名文件。
- 清单：`native-migration-manifest.json`。清单证明迁移时来源，后续实现允许文件正常变化。
- 增加可重复构建与测试脚本 `scripts/native-check.ps1`。
- DevEco HAP 与 HAR 构建成功。直接运行 scheduler HAR 本地测试：26 项通过、0 失败、0 错误、0 忽略；报告摘要归档于 `verification/stage1-scheduler.txt`。
- 纠正旧测试命令：`entry@test` 实际仅运行应用的 1 个模板测试，不能证明 scheduler 的测试通过；脚本改为直接运行 scheduler 并检查新报告、用例数和失败数。此前报告的测试覆盖结论不直接沿用。
- 保留旧代码的数据库异常处理、弃用 UI API 等构建警告。未完成手机签名、安装、传感器及性能验收。
- 迁移后少量文本仅清理行尾空白；清单记录的是最初复制哈希。

## 协作者代码复用范围

参考提交：Cixin `0f50f8c6b8897171eaf13733fe0c8c7fb6a9d4b5`（2026-09-15）。

复用 `GuardedExecutionService` 的执行前再准入、发出取消后等待 Handler 结束才释放占用等机制；适配为鸿蒙 HAR 的原生执行流程，并增加针对旧问题的回归测试。保留来源和语义映射，不直接引入 NestJS 装饰器、Node AbortController、Linux 进程守护或板端资源阈值。

资源预测与云端规划不是本阶段手机调度的前提，暂不迁入。已有 `services/api-server` 是此前独立复制资产，不代表新增手机能力。

## 阶段二：计划一致性与停止确认（2026-09-20）

- 修复执行闭包捕获旧计划：执行器和预热函数均接收出队时的最终计划。
- 状态、调试覆盖、模式变化后重新评估队列；出队前再检查，验证执行器支持配置。
- 分开手动暂停和策略暂停，保护解除可自动恢复策略暂停任务，不擅自解除手动暂停。
- 引入 STOP_REQUESTED，取消和超时后等待执行器结束才释放串行槽位；保留首个停止原因并丢弃迟到结果。
- 通用回调支持 CancellationSignal；取消发生于预热期间时不再启动推理。
- 平均延迟和 P95 改为端到端时间，日志记录停止请求时间。
- 首轮 HAP/HAR 构建成功，35 项 HAR 测试全部通过；新增 9 项针对上述行为的回归用例。最终提交前再次 HAP/HAR 构建及 35 项测试通过（0 失败、0 错误、0 忽略）。用例结果见 `verification/stage2-scheduler.txt`，构建产物哈希见 `verification/stage2-build.json`。
- 测试是 DevEco 主机本地单元测试，执行器用可控制的工作门闩验证调度语义，不等于手机资源/功耗测量。
- 运行中后台任务分块让出、排队截止时间、实际限速、实验数据导出与真机验证留待后续阶段。

协议及源码来源见 [执行协议与复用说明](04-手机调度执行协议与复用说明.md)。

## 阶段三：语义调度 SDK 同步（2026-09-20）

- 来源：外层工作区 `HarmonyOS/shopping-assistant` 中已完成的语义调度改造；同步目标为当前分支 `harmony-agent/apps/harmony`。
- 同步内容：`SchedulerClient`、`TaskTemplate/TaskContext/TaskSignal` 契约、`SemanticPolicy`、语义工作流测试、应用侧 `ShoppingTaskService`、三节点文本搜索接入、分块索引预热、SDK 接入说明及验收记录。
- 路径适配：外层 `shopping-assistant/apps/entry` 与 `shopping-assistant/apps/scheduler` 分别落到 `apps/harmony/entry` 与 `apps/harmony/scheduler`；文档和脚本路径改为当前 `harmony-agent` 结构。
- 保留边界：未把购物模型、商品数据或页面类型引入 scheduler HAR；`PrivacyGuard.ets`、旧阶段安全测试等分支已有资料继续保留，不作为本次强制删除项。
- 清理同步误带的源项目忽略产物：`rawfile/licenses/` 与 `rawfile/productConfig.json` 未纳入提交。
- 验证：`./scripts/test-semantic-scheduler.ps1 -DevEcoHome 'E:/DevEco/DevEco Studio'` 通过，65 项 Hypium 用例全部通过；脚本会先执行 `ohpm install` 并检查 Hypium Failure/Error。
- 构建：`scheduler` 独立 `assembleHar` 通过；`entry` 使用 `-c properties.enableSignTask=false assembleHap` 的不签名构建通过。产物哈希见 [语义调度验收记录](testing/04-semantic-scheduler-acceptance.md)。
- 仍未完成：没有连接真实手机，未执行实时交互、主动搜索和后台预热三类真机验收；资源预测仍是可解释先验加 EWMA，不是已训练的调度神经网络。

## 阶段四：受约束策略闭环 V1（2026-09-21）

本节记录本地分线提交 `f77b811` 的历史结果；下方协作分线与本节均从阶段三基线演进，并非依次覆盖。最新整合状态见阶段五。

### 用户要求与执行范围

- 按 [09-HarmonyOS受约束策略闭环实施方案](09-HarmonyOS受约束策略闭环实施方案.md) 修改当前 `harmony/mobile-scheduler` 分支；允许根据工程实际改进方案，同步项目日志。
- 应用接入保持解耦：App 提供业务语义、质量/隐私约束、合法执行配置及执行器；HAR 仅处理结构化契约和不透明负载。HAR 不依赖开发板、本地大模型、购物数据或页面。
- 前台决策全部本地完成；云端测试控制面为可选项，本轮不引入网络依赖，也不上传任务输入与输出。

### 已落实的修改

- 新增 ExecutionProfile/TaskCapabilityManifest、OBSERVE/SHADOW/CANARY/ACTIVE、版本化本地策略、体验目标、结构化反馈与配置审计契约；旧模板兼容。
- ConstrainedPolicy 只选择完整声明的配置，先应用质量、设备保护、可用后端、前后台和时限约束。相邻切换、冷启动门槛、冷却、有限试验期、稳定分流、本地劣化停止及停用开关已接入真实运行时。
- 影子建议独立记录，实际执行始终接收基线配置。执行器无法支持或上报不一致时失败/熔断，后续任务回退；临界状态丢弃结果，取消确认异常记录为 RUNNING，不伪装成已停止。
- 反馈由 App 页面呈现，默认关闭，用户可忽略；结果展示、使用、失效和结构化答案按 taskRunId 关联。只保留枚举选项，速度与质量反馈分开统计，不修改 App 声明的 targetLatencyMs 或质量底线。
- 增加 App 私有 Preferences Provider，保存熔断、停用与限频摘要；读写故障不会导致前台等待网络，失败时停用优化。有限遥测与分组 P50/P95 可本地导出。
- 设备状态增加来源/时间戳/未知标记；对照本机 SDK 将 hidebug 内存单位固定按 KB 换算，周期刷新安全状态。
- 购物执行器注册编码、检索、预热配置；默认和保护回退均保留全量 2700 件/256 维检索。64/16 维配置待质量评测，仅用于影子建议；后台可按 128/32 条分块。
- 取消受控检索失败后的静默串行替代，避免实际执行与上报配置不符。TaskPool 分区数与 MindSpore 线程配置明确区分，均不等于操作系统实际线程采样。
- 控制台展示模式/版本/基线/影子/执行确认/预测与实际/回退/反馈及分组摘要；默认没有 CANARY/ACTIVE 快捷放量按钮。

### 测试与审阅中修正

- 恢复原本未被 List.test 引用的 9 项 ExecutionSafety 回归测试，按当前 RUNNING + 协作取消协议更新断言；重新验证槽位占用、预热取消及回调取消信号。
- 兼容函数执行器恢复 CancellationSignal 参数，低层总时延指标修正为端到端；传给执行器和日志回调的计划使用快照，避免外部引用修改内部审计。
- 修正测试脚本：检查报告更新时间、声明/实际用例数、失败/错误/忽略数，并恢复环境变量，防止“构建成功但部分测试未运行”。
- 分块间配置、策略版本或状态桶变化的任务标记 MIXED，不把完整时延归到最后一个配置，也不纳入单配置实验比较；运行中任务的错误熔断阈值保持原策略版本。
- 最终回归 117 项全部通过（原 65 + 恢复 9 + 新增 43），0 失败/错误/忽略。独立 HAR 与不签名 HAP 构建成功，差异空白检查通过；产物哈希见 [本轮验收记录](testing/05-constrained-policy-acceptance.md)。
- 版本切换用例初次缺少启动同步点，任务在排队时正常采用了新版本；补充执行启动门闩后验证了真正的运行中版本保持。

### 仍需外部验证

尚未完成手机上的配置执行、传感器可用性、反馈页面视觉验收、质量召回与资源/功耗实验。没有神经网络调度器、签名云端策略控制面或真实因果收益结论。设备阈值与目标时延是当前工程保护/体验设定，不代表已测得最佳值。方案中“已验证设备边界”的最终验收必须由目标真机数据补齐。
## 协作分线：版本整合与完整回归（2026-09-21）

本节保留新拉取提交 `4ccaa19` 的历史结果；76 项报告及 `verification/stage4-*` 是该分线的证据，不是合并后受约束闭环的完整报告。

- 快进同步协作者提交 `d7c1a2e`，保留语义模板、任务上下文、串行工作流、去重、检查点与审计能力。
- 发现阶段三测试入口遗漏了已有的 9 项安全回归；恢复入口。恢复 `STOP_REQUESTED`、停止请求时间及回调的 CancellationSignal 参数，将取消、超时、过期、设备保护和关闭统一接入停止协议。执行器结束之前不释放运行槽位，保留第一个停止原因。
- 恢复全局暂停期间的队列重评估、执行器支持检查；保留手动暂停语义。平均延迟和 P95 再次统一为端到端总耗时。
- 旧安全测试中“HOT 固定 1 线程”的断言与新加权策略不一致，更新为当前 2 线程结果；保护自动恢复用例显式选择自适应模式。保留全部原有安全目标，并增加每项测试前的清理，避免首个失败污染后续测试。
- 新增“取消后执行器再抛超时仍保留取消原因”和“过期请求停止时仍持有执行槽”两项测试。
- 两个测试脚本共享严格门禁：报告必须新鲜、源码声明数与实际执行数一致、至少 74 项、无失败/错误/忽略。第一轮报告 74 项中 65 通过、1 失败、8 错误，第二轮 76 项中 75 通过、1 失败，均被脚本正确拒绝；修正旧测试前提与隔离后，最终 **76 项全部通过**。
- 使用本机 DevEco Studio（HarmonyOS 6.0.2/API 22 工程）完成 HAP/HAR 构建。报告见 `verification/stage4-scheduler.txt`，产物摘要见 `verification/stage4-build.json`。HAP 为未签名产物。
- `hdc list targets` 返回 `[Empty]`，本阶段没有执行手机安装或性能实验；运行结果仍是主机调度逻辑测试，不能替代真机资源、能耗与模型效果验证。

下一步执行文档 09 的手机验收清单。面板中的续航、无样本吞吐等展示仍包含估计/占位逻辑，正式演示与性能报告前需明确标识或替换为实测，不作为本阶段的性能证据。

## 阶段五：拉取冲突整合与闭环回归（2026-09-21）

- 用户要求：在当前分支整合新拉取文件并提交，不覆盖已完成的受约束闭环，继续更新项目日志。范围仍仅为 `harmony-agent`；本次提交本地分支，不包含额外推送。
- 合并来源：本地 `f77b811` 与拉取的 `a46d07e`（含 `4ccaa19` 执行安全修复及手机前功能核查），共同基线为 `d7c1a2e`。逐项解决 6 个冲突文件；保留双方能力和历史验证证据，没有使用单侧整文件覆盖。
- 停止协议统一为 STOP_REQUESTED，保留首次原因与时间；闭环取消看门狗在该状态下继续计时、熔断并记录未确认，执行器真正结束前不释放槽位。关闭期间看门狗使用仍存活的状态控制器快照。
- 保留全局暂停期间保护重评估。修正执行器检查捕获旧实例的问题，按最终配置重新查询注册表；manifest 配置不支持时先熔断并重评估合法 fallback，再决定执行或拒绝。
- 修复自动合并产生的重复测试套件注册；两个测试脚本共用严格原生门禁，语义入口保留业务依赖扫描、构建日志和通过后的结果归档。面板显示等待停止及端到端 P95。
- 新增 3 项合并交叉回归：排队配置改变后切换到另一执行器、排队配置失效后使用合法回退、全局暂停期间临界状态立即拒绝。保留上游新增 2 项停止测试和原有 43 项闭环测试，更新看门狗断言。
- 首轮 122 项中 121 通过、1 失败，门禁正确拒绝；原因是合并后的旧断言将“低电量叠加高温”按单独高温预期处理。依据现有加权策略修正为 1 线程，并断言原提交配置为 4 线程；单独高温 2 线程由其他用例覆盖。重跑 **122 项全部通过，0 失败/错误/忽略**，逐项名称核对无遗漏/重复，HAR 与未签名 HAP 构建成功。
- 文档区分 76 项协作基线、117 项闭环基线与当前结果；更新缺口清单中已被 manifest 路径部分补齐的内容，保留演示数据真实性、App 导出/实验工具、后台公平性/限流及真机验收等未完成项。没有删除新拉取的规划/核查文件。
- `hdc list targets` 为 `[Empty]`，没有手机安装或性能收益结论。最新命令、报告和产物哈希见 [合并验收记录](testing/06-merge-integration-acceptance.md)。
## 协作分线：手机验证前的软件缺口补齐（2026-09-21）

以下是新拉取 `cc12ad4` 的历史实现和验证记录，与上一节从同一 `a46d07e` 基线分叉；不能用 87/10/4 项报告代替保留受约束闭环后的本地完整回归。当前整合以阶段六为准。

- 语义路径加入升档稳定窗口；保留较低档时重新运行模板约束检查，防止旧控制器强行生成不支持的线程数或突破质量底线。调试注入/清除不再绕过稳定历史，清除覆盖时立即恢复真实设备保护。
- 快照增加各资源字段 sampledAt；电池/充电/温度 30 秒过期，CPU/内存 3 秒过期。未知与注入状态分别处理，调度器每秒检查过期，适配器每秒刷新真实状态。依据本机 SDK 的 hidebug.SystemMemInfo 单位定义，将内存从 KB 明确转换为 MB，删除按数值量级猜单位。
- 可运行任务等待 30 秒后按等待先后获得下一空闲槽，保护暂停不被越过。撤销无执行预算的 THROTTLE 策略输出；不能暂停的旧后台任务在保护条件下拒绝准入。本版不提供速率预算型限流。
- 增加标题品牌别名及价格上下界过滤，作用于最终 Top-K 选取之前；空候选正确返回空结果。保留范围明确的规则解析，不声称通用自然语言条件理解。
- 检索回传 taskpool/serial_fallback、实际配置分区数和扫描条数；失败分区全部结束后才允许串行回退，回退样本不训练原执行计划的成本预测。检查点校验和追加逻辑提取为可测试函数。
- 清理面板的 NPU/GPU 实际加速、降频、固定功耗、吞吐、续航和未测决策耗时描述；模拟等待任务单独标识，反馈显示真实终态。镜头与框选标明未接图像识别。
- 新增 ExperimentService 和面板入口：3 模式 × 2 后台条件 × 3 查询 × 3 重复，共 54 条真实搜索；后台使用现有 128 行分块缓存。支持停止、离页停止、保存已有记录及系统文件选择器导出 JSON。事件独立采集，上限 20000，溢出计数明确，不依赖 SDK 滚动窗口。
- 新增电脑 CSV 汇总工具：成功率、分终态 P50/P95、节点排队时间、截止时间违约率、后台行吞吐及可选人工标注质量评测。分离真实与注入来源，不伪造无标签质量或功耗。首个会话样本不冒充冷启动数据。
- 主机验证：**scheduler 87 项、entry 10 项、Python 统计 4 项全部通过**；HAP/HAR 构建通过。测试门禁覆盖两个模块，校验新鲜报告、声明数和实际执行数、零失败/错误/忽略；业务依赖扫描通过。
- 证据：`verification/stage5-scheduler.txt`、`stage5-entry.txt`、`stage5-summary-tests.txt`、`stage5-build.json`。保留旧 SDK 弃用/异常处理等构建警告；HAP 未签名，未做手机安装和文件选择器实测。
- 新增 [手机验证操作手册](11-手机验证操作手册与结果回传.md)，包含环境记录、14 项功能验收、真实实验、质量标注、CSV 汇总和问题回传模板。手机验证由协作者执行，原始结果另存，不以主机测试代替。

阶段五处理的是已确认的软件缺口；真实设备运行效果、质量标注、功耗与性能收益仍须实际采集。检查点仅进程内；不增加 GPU/NPU、内核调度、图片识别等额外功能范围。

## 阶段六：保留受约束主策略并整合资源与实验能力（2026-09-21）

- 用户再次拉取后继续整合，并明确要求“之前 MD 的策略要保留或者结合新拉取的创新”。本次以文档 09 为主框架，合并本地 `4e409af` 与 `cc12ad4`；只修改 harmony-agent，不推送远端、不删除双方已有功能。
- 保留 manifest 完整配置、质量/隐私硬约束、OBSERVE/SHADOW/CANARY/ACTIVE、预测与反馈限频、熔断/停用/本地持久化、STOP_REQUESTED 和取消看门狗。逐项对照表补入文档 09 第 14 节；旧三档 PolicyMode 不能覆盖受约束配置。
- 合入未带 manifest 的语义防抖、公共状态逐字段 TTL、后台等待 30 秒的公平准入；原闭环 10 秒观测保护继续生效。保留来源与时间戳，修复部分调试覆盖暴露旧真实值和快照元数据引用问题；内存转换保留向下取整，避免将红线以下内存向上舍入为安全值。
- 应用合入品牌/价格过滤、检查点校验、分区结果汇总和执行路径遥测。参数签名兼容原严格回退开关，真实受控搜索仍不静默改为串行；分区提交同步失败也等此前工作结束再返回，避免提前释放执行槽。
- 合入采样会话、取消、系统文件选择器导出及 CSV/质量统计；修正旧模式标签与实际闭环模式不等价的问题。schemaVersion=2 记录实际策略与配置、后台 workflowId，汇总区分策略/模型版本、配置、组别、执行路径及前后台注入来源，不能仅凭旧 mode 标签宣称三策略性能收益。
- 按原 MD 的最小遥测要求收紧默认导出：只含预设 queryId 与诊断字段，无搜索原文或结果 ID；独立质量评测需单独勾选固定公共语料 externalId。HAR 仍不读写导出文件、不上传、不接收购物类型；本次没有训练新模型或增加云端控制面。
- 保留上轮全部 45 项闭环回归，新加 2 项形成 47 项；新加状态元数据/部分覆盖 2 项、应用导出边界 2 项、统计分组/质量授权 5 项。完整结果为 scheduler **137**、entry **12**、Python **9** 项通过，零失败/错误/忽略；ArkTS 用例名称逐项核对无重复或遗漏。
- HAR 和未签名 HAP 构建成功。`hdc list targets` 为 `[Empty]`，未做真机交互、采样会话、文件选择器、性能或质量验收。协作分线 stage5-* 证据保留为历史；本轮报告、产物哈希及剩余边界见 [第二轮整合验收](testing/07-resource-experiment-integration.md)。

## 阶段七：HTML 转原生页面及回答到参数链路（2026-09-21）

- 用户要求：转换新增三个文件夹中的 HTML 手机前端为鸿蒙 ETS，并完善“问题回答 -> 参数调整”。只调整当前分支的 harmony-agent，保留三个原始设计文件夹，不自动提交或推送。
- 三个页面转换为 AssistantHome、SearchResults、ProductDetail 原生组件，复用 CommerceControls 与 ExperienceFeedback；Index 统一管理实际搜索、详情、清单、历史、取消、重试、排序、高质量请求和同意开关。既有调度控制台入口保留，不用 WebView 加载原 HTML。
- 保留界面风格，改用实际商品数据和原生系统图标；删除假匹配率、按倍率伪造平台价格、假库存/折扣/曲线等展示。系统选图只提供图片预览，不把图片偷偷替换成固定文字查询；相机实时拍摄、语音、图像识别、价格历史与穿搭聚合仍未实现。
- 新增通用 FeedbackReceipt/FeedbackCalibration、SchedulerClient.submitFeedback 和 App 适配方法；旧 USER_FEEDBACK 信号兼容。回执与审计关联输出节点，区分拒绝、仅记录、积累、校准，返回样本数与前后权重。拒绝越权、重复、过期和旧邀请；撤销再授权不能恢复旧邀请，忽略/取消/离页失效。
- 按版本、能力/模型、任务/状态/输入桶和实际配置分组，对速度与质量独立校准，系数分别限定为 5..10、3..9，配置惩罚至多 3。各维度独立满足最小样本量，窗口好转可恢复基础值，不把未回答当满意，不混算各维度以绕过门槛。
- 反馈权重已进入后续效用计算，不改 App 截止时间、队列优先级、质量底线或拼装配置。保留 OBSERVE/SHADOW 基线执行及 CANARY/ACTIVE 质量/样本/冷却/熔断门槛；明确首个回答不代表立即调优。默认 10 条与原限频/进程内样本限制保持，短会话通常只积累，满意概率曲线和跨启动学习尚未完成。
- 原 137/12 项回归保留，新增调度 14 项、应用 5 项。最新结果：scheduler **151**、entry **17**、Python **9** 项全部通过，原生零 Failure/Error/Ignore；脚本门禁验证用例数、报告新鲜度和业务依赖扫描。包含合成样本进入后续效用但 OBSERVE 执行配置/优先级不变的政策层回归，不能当作用户因果收益证据。
- HAR 和未签名 HAP 构建通过，未配置签名；原有数据库/模型 API 异常提示与调度页面旧 API 弃用警告保留。本轮新页面使用 UIContext 的路由与提示接口。
- `hdc list targets` 为 `[Empty]`。尚未做手机截图、键盘/大字体适配、拖拽、系统选图或外部链接运行验收，也未证明真实推理效果与性能收益。页面对应、链路和手机步骤见 [原生页面转换与反馈参数链路](12-原生页面转换与反馈参数链路.md)，原始测试报告为 testing/semantic-test-result.txt、testing/entry-test-result.txt。

## 阶段八：同页实时调度仪表盘与初始参数提问（2026-09-22，历史实现）

- 用户要求：操作 App 时同时打开新增 HTML 仪表盘呈现真实调度信息；核查需要调整初始参数时为什么不弹选择题。范围仅当前分支 harmony-agent，不自动提交/推送、不删除原型。
- 核查原型实际为 `调度盘/code.html`，使用随机数演示开发板硬件，无手机数据链路。保留原始文件，将手机可采集指标与任务/计划/实际执行/反馈回执转换为 SchedulerDashboard 原生组件，明确 NPU/GPU、功率和云 RTT 暂无采集。
- 修复原入口 pushUrl 导致 Index.onPageHide 取消任务的问题；顶部仪表盘在同页展开，通过只读 SchedulerLiveFeed 订阅状态/日志并每 500 ms 刷新。冻结/关闭画面不改变任务；原独立策略实验页仍保留离页取消。
- 首轮曾加入首次文字搜索的平衡/速度/准确性题，但模拟器截图复核后确认把内部调度概念交给普通用户判断，体验不合理；该题已在下一阶段撤销，保留内部默认约束。
- 新增通用 FeedbackAvailability 与 ActiveTaskSnapshot，HAR 不引用购物类型或界面；诊断读取不消耗配额。页面呈现未授权、同类今日已询问、间隔不足、过期等具体原因；结果出来后授权可尝试获取有效邀请。原 UTC 日限额、间隔、有效期、所有权和受约束校准门槛保持不变。
- 仪表盘未初始化或读取失败显示断连，未知字段不伪造成零；深拷贝隔离 UI 修改，退出取消订阅。小高度反馈转为滚动内容，首次题目隐藏输入区并收起键盘，修改偏好先使旧任务失效。
- 验证结果：scheduler **156**、entry **26**、Python **9** 项通过，原生零 Failure/Error/Ignore；业务耦合扫描、报告新鲜度与用例计数门禁通过。新增 SDK 5 项、应用 9 项。首轮发现的 ArkTS 可选字段/联合类型及测试显式接口/完整初始化配置问题均已修正。HAR 和未签名 HAP 构建成功。以上是上一轮基线，改题后会重新记录。
- 本次发现已连接 `127.0.0.1:5555` 手机模拟器（model=emulator，1320x2856），不能沿用上一轮 `[Empty]` 结论。已覆盖安装，实际看到首次偏好题、授权后的单题、REAL/OBSERVE 下的真实搜索事件与日限额提示；没有卸载/清数据，也没有提交虚构满意度。
- 连续搜索实测发现程序清空 TextInput 触发 onChange 后误取消新任务；增加草稿相等判断后重新构建安装，`nike`、`adidas` 连续搜索均返回 20 件商品，终态窗口计数由 4 增至 7，未再出现误取消。显示 1752/1581 ms 是模拟器单次结果，不作优化收益结论。
- 测试生成一次体验邀请，保留 UTC 日频控，不重置额度；反馈开关已恢复关闭并重启回初始会话。真实手机传感器/功耗、全面大小屏/大字体与质量收益仍未验收。本轮截图与产物 SHA256 见 [实时仪表盘与提问验收](testing/08-live-dashboard-and-questions.md)。

## 阶段九：按可感知体验重新设计低频提问（2026-09-22）

- 用户反馈：普通用户无法判断 300/500/800 ms、质量档位等内部参数；提问应结合 App/系统已知信息，只询问无法确定的等待、发热、卡顿体验，并按优先级低频进行。
- 删除首次搜索参数题及其原生组件，普通搜索直接使用保守默认 TaskContext；`SearchPreferences` 仅保留内部兼容构造，不再暴露调度参数。回归文档和旧截图标记为历史，不作为当前交互说明。
- 用户进一步纠正：温度、内存占用和卡顿应该自动监测，不需要问用户。最终撤销发热/卡顿题和新增流畅度问卷维度；客观指标缺失不变成问卷，内存占用不冒充帧卡顿实测，既有设备保护无需等待作答。
- FeedbackController 仅按业务迹象选择主观问题：实测软截止超时优先问等待能否接受，应用明确上报质量不确定时才问是否符合需求；没有必要不出题。一次只生成一题，保留授权、结果展示、taskRunId、2 分钟过期和原有日/能力/间隔限频，优先级不绕过限频。
- 新增通用 AppExperienceObservation，接收经过类型/范围校验的结果准备耗时、质量疑点与已有有用性判断，脱离页面和商品类型。当前 App 实际上报耗时，质量疑点接口只由合成用例验证，不伪造置信度；消费事件可减少问卷，但不充当正面训练样本。邀请原因与提问优先级记录在仪表盘事件中。
- 反馈回执不再向用户展示时延权重和样本数字，只说明已记录、继续积累或仍受安全边界约束。等待/质量仍各自有界校准，不由一次回答直接调参；进入商品详情时不覆盖体验题。
- 调度盘入口改为购物页右上角可见的“调度盘”按钮，也可在“我的”页面点击“打开实时调度盘”；打开后同页展开，不跳转、不取消当前任务。
- 交付范围为手机内实时界面，原 HTML 没有电脑浏览器到手机的联网桥接。初始目标值仍为工程设定，未训练神经调度器；短会话反馈积累、跨启动学习和目标真机收益边界不变。方案与复测步骤见 [文档 13](13-实时调度仪表盘与提问诊断.md)。
- 最终按“客观指标不提问”版本重跑：scheduler **160**、entry **26**、Python **9** 项全部通过，零 Failure/Error/Ignore，业务解耦/报告新鲜度/用例计数门禁通过；HAR 和未签名 HAP 构建成功。主观质量疑点在测试中显式上报，不依赖无条件弹题。当前 `hdc list targets` 为 `[Empty]`，本阶段未做最终版安装、截图和触控验证；上一阶段模拟器截图仅作为历史证据。

## 阶段十：迁移至电脑端实时调度监控（2026-09-22）

- 用户明确不需要手机 App 内的同页仪表盘，要求电脑开启端口以实时监测整个调度件。
- 移除 Index 顶部同页仪表盘与内嵌 SchedulerDashboard。手机“我的”页仅保留电脑地址、配对码、推送开关与连接状态。
- 新增电脑 Node HTTP 监控服务（默认 8765）和只读网页。手机向受配对码保护的 `/api/snapshot` 推送，网页读取最新快照；接收端按白名单裁剪字段，只在内存暂存一份快照与最多 24 条事件。
- 手机 publisher 复用只读 SchedulerLiveFeed，每 750 ms 最多一个在途请求；失败退避，后台暂停，回前台恢复。推送需用户在 App 明确开启，不接管调度执行或策略。
- 电脑与手机使用可信私有 LAN；本地 HTTP 不是加密链路，不可向互联网暴露调试端口。HAP 已有 INTERNET 权限。操作和待做的设备联调见 [监控说明](13-实时调度仪表盘与提问诊断.md)。
- 本轮 HAP 与 HAR 原生构建成功，电脑端 Node 脚本语法检查通过；测试套件未运行。当前无连接模拟器/手机，网络配对、网页实时刷新及断连重连尚未设备验收；哈希见 [本轮验收](testing/08-live-dashboard-and-questions.md)。
