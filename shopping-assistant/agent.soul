PROJECT SOUL: 智能比价助手

1. 身份与目标

这是一个面向评委可自行体验的智能比价项目。
当前唯一目标不是“展示很多想法”，而是交付一个可安装、可访问、可跑通、可降级的完整 MVP。

核心产品定义：
- 用户拍一双鞋
- 系统返回多平台候选结果
- 用户继续用自然语言表达要求
- 系统在同一会话中持续收敛结果

项目口号：
- Just say the word.

2. 最高优先级

所有 Agent 必须把以下顺序看得比功能数量更重要：

1. 主链路不断
2. 会话连续
3. 候选结果可比较
4. 评委可自行下载安装并体验
5. 外部依赖失败时仍可演示

任何工作如果损害这 5 条，即使“更高级”也不应优先。

3. 权威顺序
发生冲突时，按以下顺序裁决：

1. 用户最新明确指令
2. topic 对应的权威文档
3. 本文件

当前 topic 权威文档如下：
- 产品定义：`docs/00-项目定义与范围.md`
- MVP 边界：`docs/01-MVP定义.md`
- 模块与数据流：`docs/02-功能模块分块与数据流.md`
- 职责边界：`docs/03-模块职责边界.md`
- 数据对象：`docs/04-模块输入输出与核心数据对象.md`
- MVP 优先级：`docs/05-MVP模块映射与优先级.md`
- 技术方案：`docs/06-技术方案与实现策略.md`
- 评委入口：`docs/07-评委分发与入口方案.md`
- 存储方案：`docs/08-数据库与缓存设计.md`
- 接口契约：`docs/09-接口草案与数据契约.md`
- 目录结构：`docs/10-项目结构与目录规划.md`
- 开发排期：`docs/11-开发顺序与里程碑规划.md`
- 测试验收：`docs/12-测试与验收清单.md`
- Adapter 解耦：`docs/21-Adapter分层与数据功能解耦方案.md`

3A. 当前最高优先架构口径（2026-06-01）

本节覆盖旧文档中仍残留的 MVP 早期设想。所有 Agent 必须按本节执行。

当前项目已从“证明 MVP 骨架”进入“后端真实链路与数据注入准备”阶段。后续方案不能再以早期 MVP 降级口径替代最终可用链路。

实时服务边界：
- 所有用户实时链路必须在云端可用，不能依赖开发者本地电脑。
- 前端只能连接云端 API，不知道也不依赖本地 GPU Worker 是否在线。
- 云端承载：图片上传、COS 存储、用户图主体粗定位、框选结果返回、用户手动框选更新、用户图裁剪标准化、豆包 embedding、ANN 检索、多轮对话、候选返回。
- 云端只能部署轻量即时能力；不能把重型本地 GPU 模型作为在线必要依赖。

离线服务边界：
- 本地 GPU 只用于离线准备和重算力批处理。
- 本地 GPU 可用于：商品图主体提取、商品图标准化、商品图 embedding 批量生成、商品池 embedding 重建、爬虫数据入库前处理。
- 本地 GPU 输出必须回写到云端可用的数据形态，例如 COS 图片、SQLite/线上数据库中的 embedding 记录或可导入文件。
- 本地 GPU 不得成为用户实时搜索链路的必需环节。

3B. Adapter-first 解耦口径（2026-06-02）

所有 Agent 必须遵守：

- 所有功能入口必须先经过 adapter，再进入核心业务逻辑。
- 核心模块只能消费标准 contract，不得直接解析外部原始数据。
- 爬虫、八爪鱼、人工样本、官方 API、前端表单、COS、豆包、DeepSeek、SQLite 查询结果都属于外部来源。
- 外部来源差异必须被隔离在 adapter 层。
- rawPayload 只用于回溯、调试和审计，不能成为核心功能判断的主要输入。
- 新功能必须先定义 Port / Contract，再实现具体 Adapter。
- 缺数据是数据覆盖问题，不是功能设计问题；不得因为某批数据缺字段就改核心业务结构。
- 任何 adapter 改造都必须非破坏式推进：旧 API 不删、旧响应字段不删、现有主链路先保持可用。
- 当前已完成的第一批 adapter 化落点包括：图片上传、商品导入、商品导入批次状态、商品入库持久化、商品后台编辑、商品后台输出、商品导入批次输出、商品图片内容读取、商品图向量化、商品 embedding 写库结构、商品原始载荷解析、来源属性商品打标、已有商品标签投影、商品入库模型打标、会话商品画像识别、会话详情输出、搜索 query embedding、商品搜索信号派生、商品池搜索结果输出、候选视觉复核、用户图 bbox 解析与校验、用户图内容读取与裁剪、用户图预处理快照输出、搜索进度事件输出、多轮对话意图解析、多轮上下文输出、ANN 检索、候选写库、候选输出、候选 cursor、候选搜索上下文、智能建议输出、用户画像上下文、用户画像 payload 规则、用户画像输出、兜底候选输出。

当前权威方案为：
- `docs/21-Adapter分层与数据功能解耦方案.md`

商品数据路线：
- 当前主路线是 `ProductPool` 商品池，而不是聚合搜索 API。
- 淘宝、天猫、京东、得物、拼多多、抖音电商等平台数据最终通过爬虫、授权接口、半自动整理或人工样本写入商品池。
- 爬虫程序只需要按商品池导入契约输出数据；后端搜索链路不应依赖具体爬虫实现。
- 目前真正缺口是“真实平台商品数据持续注入”，不是“聚合搜索 API 供应商选择”。

用户图处理路线：
- 用户上传图实时链路必须云端完成。
- 用户图主体定位只要求足够快、框出大概商品范围，前端负责高亮展示并允许用户调整。
- 后端必须返回 normalized bbox，前端按图片实际展示尺寸绘制亮框。
- 用户调整 bbox 后，后端用最新有效 bbox 重新裁剪、重新生成用户图 embedding、重新搜索。
- 用户图 embedding 当前走豆包 embedding 在线模型，不能依赖本地 embedding worker。

商品图处理路线：
- 商品图原图用于前端展示。
- 商品图 embedding 应优先使用主体裁剪图生成，减少宣传图背景、裤子、文字、水印等干扰。
- 商品图主体提取和 embedding 可由本地 GPU 离线批处理完成。
- 商品图和用户图向量不得混用不同 provider/model/dimension，切换 provider 后必须重建对应 embedding。

4. 当前已经锁死的产品真相

- 首版只做鞋类
- 首版“同款”口径：鞋类必须严格一致
- 首版“全网最便宜”口径：不承诺数学意义上的全网最低价，只在已接入主流平台范围内返回更低价候选和该范围最低价结果
- 首版货源最低表达要求只包含：
  - 有货 / 无货 / 未知
  - 平台
- 评审希望记住的亮点：
  - 真实结果返回快
  - 用户侧无固定筛选语法
  - 结果准确
  - 前端交互惊艳

5. MVP 历史边界与当前执行边界

完整 MVP 骨架按以下口径执行；当前阶段已经进入真实链路补强，不应再用 MVP 早期口径否定已落地模块。

P0 必做：
- 用户输入与会话启动
- 商品理解
- 跨平台检索与聚合
- 结果标准化与候选池
- 结果筛选与收敛
- 会话与状态保持

P1 也进入本轮实现：
- 智能建议
- 决策辅助
- 商品详情
- 缓存、降级与恢复

当前不进入本轮实现：
- 抖音小程序
- 历史价格真实链路
- 配送时效真实链路
- 非鞋类扩展
- 应用商店分发

用户长期记忆基础模块已经进入后端能力范围。后续所有用户画像写入仍必须遵守“用户确认后进入长期画像”的原则，不得改成静默自动长期记忆。

6. 当前已经锁死的技术真相

- 客户端：Flutter
- 后端：Node.js + NestJS
- 主存储：SQLite + Prisma
- 当前 MVP 不引入 Redis
- 缓存基线：NestJS 进程内轻缓存 + SQLite 快照 + 固定 fallback 结果
- 图片存储：国内对象存储，代码层保持 provider-neutral，当前默认腾讯云 COS
- 图片上传：服务端中转
- 图片策略：双轨图片
  - 压缩识别图先走主链路
  - 原图按需补传
- 图片压缩主位置：Flutter 客户端
- 候选结构：`CandidateItem = 强结构核心字段 + normalizedAttributes + rawPayload`
- 后端部署：Zeabur
- 评委入口：Zeabur 静态服务 + 腾讯云 COS APK 下载
- 交付路径：APK + Web 展示页 + 安装说明

7. 当前未完成核心事项

当前未完成的核心事项：
- 真实平台商品数据持续注入
- 多平台采集器稳定化
- 商品池图片质量与 embedding 覆盖率提升
- 云端轻量用户图主体定位能力

聚合搜索 API 不再是当前主路线。除非用户明确重启该路线，否则不得把“选择聚合搜索 API 供应商”当作项目唯一未决项或主阻塞项。

8. 明确禁止事项

没有用户新指令时，禁止：

- 把抖音小程序拉回当前 MVP
- 把历史价格、配送时效拉回当前即时主链路
- 把用户记忆改成无需确认的静默长期写入
- 把 Redis 重新塞回当前 MVP
- 把 SQLite 改成别的数据库
- 把 Flutter 改成别的客户端栈
- 把 Zeabur/国内对象存储/服务端中转/双轨图片重新讨论一遍
- 把聚合搜索 API 重新设为商品数据主路线
- 让用户实时搜索链路依赖本地 GPU Worker
- 在最终真实链路中用 mock/hash/fallback 伪装真实识别、真实 embedding 或真实候选
- 擅自把“严格同款”放宽成近似款
- 擅自把“货源最低表达要求”扩成首版必须展示更多字段
- 擅自把接口骨架改成无会话式搜索
- 为了“更工程化”拆出多套后端逻辑

9. 当前工程目录真相

仓库按以下结构推进：

- `apps/mobile-flutter/`
- `apps/judge-entry-web/`
- `services/api-server/`
- `docs/`
- `samples/`
- `scripts/`
- `infra/zeabur/`

后端唯一服务目录：
- `services/api-server/`

评委入口目录：
- `apps/judge-entry-web/`

10. 后端架构真相

后端必须保持以下分层：

- API 层
- 应用编排层
- 领域模块层
- 外部适配层
- 存储 / 轻缓存 / 降级层

必须存在的业务模块：
- assets
- sessions
- product-profile
- search
- candidates
- turns
- suggestions
- details
- fallback

必须存在的外部适配器：
- image-asset-adapter
- model
- search-provider / product-data-provider
- storage
- embedding-provider
- image-preprocessor
- product-import-adapter
- query-image-preprocess-adapter
- ann-search-port
- candidate-item-adapter
- candidate-view-adapter
- candidate-cursor-adapter

缓存抽象目录必须保留：
- `cache/interfaces/`
- `cache/implementations/`

即使当前不用 Redis，也不能把缓存抽象删掉。

11. 图片链路真相

必须遵守：

- 压缩识别图作为主链路图
- 原图按需补传
- 国内对象存储分 bucket，当前 provider 为腾讯云 COS
- bucket 角色固定：
  - compressed-recognition
  - original-source
  - demo-assets
- 对象 key 前缀固定：
  - `{yyyy}/{mm}/{dd}/{sessionId}/{assetGroupId}/`

用户实时图像处理补充：
- 用户图上传、主体定位、框选返回、裁剪标准化、豆包 embedding、ANN 检索都必须在云端完成。
- 云端主体定位模型必须轻量，目标是快速给出大致框选，不追求像素级分割。
- 后端返回给前端的框选必须使用 normalized 坐标。
- 前端可调整框选范围；后端以最新有效框选作为用户图 embedding 输入。
- 本地 GPU Worker 不属于用户实时链路。

商品离线图像处理补充：
- 本地 GPU Worker 只服务商品池离线准备与批量 embedding 重建。
- 商品图预处理结果和 embedding 元数据必须记录 provider、model、dimension、bbox、置信度、是否回退原图。

12. 接口真相

当前 P0 接口：
- `POST /api/v1/assets/images`
- `POST /api/v1/sessions`
- `GET /api/v1/sessions/{sessionId}`
- `POST /api/v1/sessions/{sessionId}/turns`
- `GET /api/v1/sessions/{sessionId}/candidates`
- `GET /api/v1/sessions/{sessionId}/search-events`

当前 P1 接口：
- `GET /api/v1/candidates/{candidateItemId}`
- `GET /api/v1/sessions/{sessionId}/suggestions`
- `POST /api/v1/sessions/{sessionId}/subject-selection`
- `POST /api/v1/sessions/{sessionId}/candidates/more`

当前接口定稿口径：
- 双轨图片共用同一上传接口
- 详情按 `candidateItemId` 读取
- 候选池支持基于 cursor 的“查看更多”，复用当前 session、筛选状态和 query embedding
- 金额字段用 `{ amount, currency }`
- 建议卡片用固定 `actionType + params`

13. 数据与状态真相

正式状态必须落 SQLite：
- QuerySession
- ImageAsset
- ProductProfileSnapshot
- SessionTurn
- FilterSnapshot
- CandidateSnapshot
- CandidateItem
- ExternalSnapshot / FallbackSnapshot

Agent 不得把正式状态只放进内存。

14. 降级与真实链路真相

降级不是备选项，是 MVP 组成部分。

必须保证：
- 商品理解失败时有兜底
- 搜索 provider 失败时有兜底
- 会话能回到最近一次有效状态
- 前端不能因为外部失败直接卡死

最终真实链路补充：
- 生产/最终链路缺少视觉模型、embedding provider、COS 或商品池数据时，应返回明确错误或明确降级原因，不得伪造真实结果。
- mock、hash embedding、固定 fallback 只能用于开发、演示兜底或明确标记的 demo 模式。
- 最终候选结果必须能从返回数据中追踪 provider、model、embeddingId、recallSources 和是否降级。

15. 交付真相

最终交付不是源码，而是：

- 可访问的 Web 展示页
- 可下载的 APK
- 可安装的 APK
- 可连接线上后端的 App
- 可完成完整主链路的体验

16. 验收真相

项目只有在同时满足以下条件时才算“可交付”：

- Web 展示页可访问
- APK 可下载
- APK 可安装
- 新装后可打开
- 用户可完成一次鞋类查询
- 用户可进行至少两轮自然语言追问
- 候选结果会刷新
- 建议、决策辅助、详情都可见
- 降级路径存在且可跑
- 评委无需额外解释环境配置即可体验

17. 多 Agent 工作规则

所有 Agent 必须遵守：

1. 不要重开已定决策
2. 不要越过文档边界擅自扩 scope
3. 做实现时优先维护主链路和评委体验
4. 改动一个基础约束时，必须同步检查相关文档是否冲突
5. 无法确定时，宁可保守遵守现有文档，不要自创新方案

18. GitHub 协作与推送协议

GitHub 仓库权威地址：
- `git@github.com:xieluyao60-coder/shopping-assistant.git`

### 18.1 仓库绑定规则

所有 Agent 必须把上面的仓库视为唯一权威远端。

当前工作区应已经绑定到该仓库。

每次开始 Git 相关操作前：

1. 先执行 `git remote -v`
2. 如果 `origin` 是 `https://github.com/xieluyao60-coder/shopping-assistant.git`，必须先执行：
   - `git remote set-url origin git@github.com:xieluyao60-coder/shopping-assistant.git`
3. 再执行 `git fetch`、`git pull` 或 `git push`
4. 不得触发 GitHub 账号选择弹窗
5. 不得使用 HTTPS 登录
6. 不得把 token 写进 remote URL
7. 除非用户明确要求，不得修改 GitHub 凭据、全局 credential helper 或系统代理设置

### 18.2 每次开始新模块前的必做检查

任何 Agent 在开始一个新模块、一个新页面、一个新服务能力之前，必须先做以下检查：

1. 检查当前是否处于 Git 仓库中
2. 检查当前分支
3. 检查 `git status --short`
4. 检查是否存在未提交变更
5. 检查是否存在未同步的远端更新

最低动作要求：

- 看当前工作区是否干净
- 看当前是否有他人或其他 Agent 的新提交
- 看自己将要开始的模块是否会覆盖尚未整理的旧改动

如果发现：

- 本地已有未整理改动
- 远端已有新提交
- 当前分支用途不清晰

则不得直接开始新模块编码，必须先整理现场。

### 18.3 分支策略

默认规则如下：

1. `main` 只承接可交付、可回归通过的内容
2. 新模块、新能力、新一段独立任务，优先使用独立分支
3. 分支命名应直接表达任务范围

建议格式：

- `feat/<module-name>`
- `fix/<module-name>`
- `docs/<topic>`
- `refactor/<module-name>`

示例：

- `feat/session-flow`
- `feat/candidate-list`
- `fix/image-upload`
- `docs/mvp-finalization`

如果用户明确要求直接在当前分支推进，以用户要求为准。

### 18.4 什么时候应该推送

Agent 不应把推送当成“随手存档”，而应把它当成“发布一个可识别、可回滚、可继续协作的稳定切片”。

必须推送的时机：

1. 完成一个独立模块闭环后
2. 完成一个里程碑日目标后
3. 完成一次会影响多人协作的基础改动后
4. 在准备切换到下一个大任务前
5. 在用户明确要求同步到 GitHub 时

不应推送的时机：

1. 主链路明显已坏
2. 构建明显失败且未解释
3. 临时调试代码还没清理
4. 核心文档和代码状态不一致
5. 明知会覆盖或污染他人工作而尚未整理

### 18.5 推送前最低检查

任何推送前，至少完成以下检查：

#### 通用检查

1. `git status --short`
2. `git diff --stat`
3. 人工复核改动文件列表是否符合当前任务范围
4. 检查是否误带：
   - 密钥
   - token
   - `.env`
   - 本地缓存
   - 大体积无关文件

#### 文档检查

如果本次改动涉及文档：

1. 文档之间口径一致
2. 不保留明显过期描述
3. 不把未定项写成已定项

#### 后端检查

如果本次改动涉及 `services/api-server/`：

1. 至少完成一次可启动检查
2. 若改动接口或数据结构，至少做一轮对应接口自测
3. 若改动 Prisma，至少做 schema / migration 合法性检查
4. 不允许在已知会破坏现有接口契约的状态下直接推送

#### Flutter 检查

如果本次改动涉及 `apps/mobile-flutter/`：

1. 至少完成一次可启动检查
2. 至少检查主页面无明显崩溃
3. 若改动主链路页面，至少手工跑一遍当前链路

#### 评委入口页检查

如果本次改动涉及 `apps/judge-entry-web/`：

1. 页面可打开
2. 下载入口未丢
3. 文案与当前交付形态一致

### 18.6 推送前强制回归原则

如果本次改动触及以下任一内容，必须至少做一次回归检查：

- 上传图片
- 创建会话
- 候选列表
- 自然语言追问
- 商品详情
- 建议卡片
- 部署配置
- Web 入口页

回归目标不是“全量重测”，而是确认：

**本次改动没有把已经能跑的旧链路打断。**

### 18.7 Commit message 规则

commit 标题必须简洁、可检索、可判断范围。

建议格式：

- `feat(module): summary`
- `fix(module): summary`
- `docs(scope): summary`
- `refactor(module): summary`
- `chore(scope): summary`

示例：

- `feat(session): add session creation and status flow`
- `feat(candidates): add normalized candidate snapshot pipeline`
- `fix(upload): repair image relay to object storage`
- `docs(agent): add github push protocol`

禁止使用：

- `update`
- `fix bug`
- `misc`
- `changes`
- 无法看出范围的空泛标题

### 18.8 Push description / 变更说明规则

每次推送后，Agent 输出给用户或写在协作记录中的说明，至少包含：

1. 本次范围
2. 影响文件
3. 是否影响主链路
4. 做了哪些检查
5. 当前剩余风险

建议模板：

- `Scope:` 本次做了什么
- `Files:` 改了哪些关键文件或目录
- `Checks:` 跑了哪些检查
- `Risk:` 还剩什么风险或未覆盖项

如果使用 PR 描述，也按同样结构写。

### 18.9 禁止推送条件

出现以下任一情况，禁止推送：

1. 明知会破坏当前主链路
2. 明知 APK / Web 入口 / 后端任一核心交付面已坏
3. 明知有未清理调试代码
4. 明知包含敏感信息
5. 明知本地未同步远端且存在冲突风险

### 18.10 推送与文档同步规则

如果推送包含以下内容之一：

- 新模块
- 新接口
- 新目录
- 交付路径变化
- 验收口径变化

则必须同步检查是否需要更新以下文档：

- `06-技术方案与实现策略.md`
- `09-接口草案与数据契约.md`
- `10-项目结构与目录规划.md`
- `11-开发顺序与里程碑规划.md`
- `12-测试与验收清单.md`

如果代码已改而文档仍旧，会直接降低多 Agent 协作稳定性。

19. Agent 交接输出格式

每个 Agent 结束时，至少交代：

- 改了哪些文件
- 哪些链路已经打通
- 哪些链路仍未打通
- 当前最大风险是什么
- 是否影响当前核心缺口：真实商品数据注入、云端用户图主体定位、商品池 embedding 覆盖率

20. 结论

这个项目现在不是“继续讨论方案”的阶段，而是：

- 主路线已基本锁死
- 文档需要跟随最新真实链路持续校准
- 商品数据主路线是商品池 + 真实平台数据注入
- 用户实时链路必须云端可用
- 本地 GPU 只做离线准备
- 所有 Agent 的职责都是在不破坏既定约束的前提下，把最终可用后端和可体验前端做稳
