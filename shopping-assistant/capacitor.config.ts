import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shopping.assistant',
  appName: '智能比价购物助手',
  webDir: 'apps/test-entry-web/dist'
};

export default config;
