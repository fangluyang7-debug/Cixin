一、前端 Android 模块
1. 拍摄上传模块
负责：

调用摄像头
选择本地图片
图片预览
图片压缩
上传图片到后端
2. API 通信模块
负责统一调用后端接口：

上传识别接口
商品筛选接口
商品详情接口
历史价格接口
用户偏好接口
3. 商品结果展示模块
负责：

展示商品卡片列表
展示平台、价格、店铺类型、相似度
展示推荐理由
点击进入详情页
4. 商品详情模块
负责：

展示商品大图
展示商品标题
展示当前价格
展示平台信息
展示购买链接
展示推荐理由
5. 历史价格趋势模块
负责：

接收历史价格数据
绘制价格折线图
展示历史最低价
展示近期涨跌趋势
展示是否值得购买
6. 自然语言输入模块
负责：

接收用户输入
例如：便宜点、只看旗舰店、预算500以内
发送给后端 LLM 模块
接收筛选后的商品结果
刷新商品列表
7. 推荐提示展示模块
负责：

展示后端返回的推荐商品
展示推荐理由
弹出“最值得买”“历史低价”“旗舰店优先”等提示卡片
8. 本地状态与缓存模块
负责：

缓存最近一次识别结果
缓存商品列表
缓存用户当前筛选条件
保存简单浏览历史
网络失败时展示上一次结果
二、后端模块
1. 图片解析模块
负责：

接收前端上传的图片
图片存储
调用云端多模态模型 / 火山引擎 OpenAPI
识别商品类目、颜色、品牌、款式、关键词
输出结构化商品信息
输出示例：

{
  "category": "运动鞋",
  "brand": "Nike",
  "color": ["白色", "黑色"],
  "style": ["低帮", "板鞋"],
  "keywords": ["白黑低帮运动鞋", "Nike板鞋"]
}
2. 商品搜索模块
负责：

根据解析结果生成搜索关键词
调用第三方商品聚合 API
获取多个平台商品结果
获取价格、店铺、图片、链接等信息
3. 商品数据标准化模块
负责把不同平台返回的数据统一成标准格式：

{
  "id": "sku_001",
  "title": "Nike 白黑低帮板鞋",
  "platform": "京东",
  "price": 399,
  "imageUrl": "xxx",
  "shopType": "自营",
  "buyUrl": "xxx",
  "historyPrices": []
}
4. 推荐排序模块
负责核心排序逻辑：

图片相似度评分
标题/关键词匹配评分
价格优势评分
店铺可信度评分
历史低价评分
用户偏好评分
综合排序
最终决定商品展示顺序。

5. LLM 交互模块
负责：

接收用户自然语言输入
解析成结构化筛选条件
判断是否需要重新搜索
生成简短推荐理由
例如：

用户输入：

只看旗舰店，预算500以内
转换成：

{
  "maxPrice": 500,
  "shopType": ["旗舰店"],
  "sortBy": "costEffective"
}
6. 用户记忆 / 偏好模块
负责：

记录用户常买品类
记录价格偏好
记录平台偏好
记录店铺偏好
给 LLM 和排序模块提供参考
MVP 可以先做简单版，只记录：

预算范围
常用筛选条件
偏好平台
7. 历史价格模块
负责：

获取或缓存历史价格
计算历史最低价
计算近7天/30天均价
判断当前价格是否划算
生成购买建议
8. 数据库与缓存模块
负责保存：

商品缓存
价格历史
识别记录
用户筛选条件
用户偏好
第三方 API 返回结果
作用：

减少重复请求
提升响应速度
降低第三方 API 成本
9. Mock / 降级模块
负责：

第三方 API 失败时返回本地 mock 商品
AI 接口失败时返回预设识别结果
保证比赛演示不断链
这个模块很重要，防止现场翻车。

三、最终交互流程
Android 拍照
↓
前端上传图片
↓
后端图片解析模块识别商品
↓
商品搜索模块调用聚合 API
↓
标准化模块统一商品格式
↓
推荐排序模块排序
↓
返回商品 JSON 给前端
↓
前端展示商品卡片和价格趋势
↓
用户输入自然语言筛选
↓
LLM 模块解析筛选条件
↓
推荐排序模块重新排序
↓
前端刷新结果